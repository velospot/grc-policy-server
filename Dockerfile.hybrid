FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1

RUN pip install --no-cache-dir "opendataloader-pdf[hybrid]>=2.4.3" docling

EXPOSE 5002

CMD ["opendataloader-pdf-hybrid", "--host", "0.0.0.0", "--port", "5002"]
